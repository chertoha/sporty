import { SxProps } from "@mui/material";

const loader: SxProps = {
  position: "fixed",
  top: "30%",
  left: "50%",
  transform: "translate(-50%, -50%)",
};

const styles = { loader };

export default styles;
