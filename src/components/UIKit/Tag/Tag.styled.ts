import { SxProps } from "@mui/material";

const chip: SxProps = {
  height: "auto",
  fontSize: {
    xs: "14px",
    md: "16px",
  },
  lineHeight: 1.5,
  py: "3px",
  px: { xs: 6, md: 8 },
};

const styles = { chip };

export default styles;
